{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur la page d'accueil

{% endblock %}



{% block body %}

{% for flash_message in app.flashes('info') %}
    <div class="alert alert-info">{{ flash_message }}</div>
{% endfor %}

<style>


    .alert-info {
        background-color: #d1ecf1;
        border-color: #bee5eb;
        color: #0c5460;
        text-align: center;
    }
    .header-right {
        text-align: right;
        padding: 10px;
    }
    .welcome-title {
        text-align: center;
        margin-top: 50px;
    }
    .cards-container {
        display: flex;
        justify-content: space-around;
        margin-top: 50px;
    }
    .card {
        padding: 20px;
        box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
        transition: 0.3s;
        width: 40%;
        text-align: center;
    }
    .card:hover {
        box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
    }
</style>

<div class="header-right">
    <a href="{{ path('app_login') }}">Login</a> / <a href="{{ path('app_register') }}">Register</a>
</div>

<h1 class="welcome-title">Bienvenue</h1>

<div class="cards-container">
    <div class="card">
        <h2>Free</h2>
        <p>Dans cette abonnement, vous pourrez tester notre offre, la création de pdf via des urls.</p>
        <p>Ici la limite sera de 1 pdf par jour.</p>
    </div>
    <div class="card">
        <h2>Premium</h2>
        <p>Ici, ......</p>
        <p>La limite sera de 10 pdf par jour.</p>
    </div>
</div>
{% endblock %}
